import { Button } from '@mui/material';

const CapabilitiesButton = () => {
  return (
    <Button variant="contained" color="error" sx={{ margin: 1 }}>
      Capabilities
    </Button>
  );
};

export default CapabilitiesButton;
