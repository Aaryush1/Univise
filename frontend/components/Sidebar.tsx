import Drawer from '@MUI/material/Drawer';
import Typography from '@MUI/material/Typography';

export default function Sidebar() {
    return (
        <Drawer variant="persistent">
            <Typography variant="button">Chat 1</Typography>
        </Drawer>
    )
}